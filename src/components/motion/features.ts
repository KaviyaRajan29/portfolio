// Loaded lazily by <LazyMotion> so Framer Motion's heavy feature bundle is a
// separate chunk, not part of the initial download. domMax (not domAnimation)
// because the navbar uses shared-layout animations (layoutId).
export { domMax as default } from 'motion/react'
