# Optional. Where the contact form POSTs submissions as JSON {name, email, message}.
# Any service that accepts a JSON POST and returns 2xx on success works, e.g.
# Formspree:  https://formspree.io/f/your-form-id
# Leave unset (or omit the file) to use the built-in simulated send — no network.
VITE_CONTACT_ENDPOINT=
